# Nix build outputs
result
result-*

# Local tool state
.codex
.codex/
.claude/settings.local.json
.direnv/
.devenv/

# Nix-managed skill files (pinned via flake.lock, placed by home-manager)
nixos-managing
